# Hybrid studies

Hybrid studies are legacy studies, with the modeler files and directories in addition: see [02-inputs](0.2-inputs)

The parameter.yml file from modeler studies is ignored, solver parameters depends on the command line as usual with legacy studies.

Currently the results are written in an output file with this format: modeler-solutions-\*-\*-\*--optim-nb-\*.txt
